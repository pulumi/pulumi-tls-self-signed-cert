{
    "name": "@pulumi/tls-self-signed-cert",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource tls-self-signed-cert ${VERSION}"
    },
    "dependencies": {
        "@pulumi/tls": "^4.0.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
